# Banking-System
The Banking System that is build using the Spring Boot which provide the Authentication, Authorization and Security using the Spring Security and Uses the WebSocket for sending the Notification to user. 
